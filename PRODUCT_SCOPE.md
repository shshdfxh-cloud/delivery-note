# Delivery Note：通用交付审查

## 产品目标

根据客户给定的最终目的，审查交付成果是否完整、事实是否有依据、是否能运行，以及是否真正实现目的；输出可追溯证据、失败项、未验证项和下一步。

**这不是网页测试器，也不只是生成一段交付说明。网页只是当前一种检查入口。** 数据、文档、脚本、软件、网站和完整系统应共用“目的—条件—检查—证据—结论”，但使用各自适合的方法。

## 当前版本与目标的区别

目前实现了通用的目的与验收条件、真实文件读取、分层证据和报告导出；第一批检查能力覆盖UTF-8文本、CSV、JSON、原始数据重算，以及离线静态网页的真实操作。

尚未实现对任意交付物的自动全面审查。当前不能把文本包含检查当成报告内容正确性判断，不能把与原始材料一致当成现实事实认证，也不能把有限网页流程当成完整系统验收。自动目标拆解、条件完整性和不同类型的执行器还需要继续验证和扩展。

## 后续开发顺序

1. 首先检验客户目的能否被正确拆解：暴露歧义、遗漏、约束和证据缺口，不能只靠勾选“已覆盖”就视为自动完成。
2. 按实际任务增加文档内容与出处审查、脚本/程序输入输出检查、服务/完整系统端到端流程，不因已有网页演示而只优化网页。
3. 让静态检查、事实核对、语义判断和实际执行使用统一证据模型；新增类型分别验证正确、错误、证据不足三种情况。

## 判定原则

能客观测量的结果使用对应工具。AI用于理解目标、阅读材料、发现矛盾、选择检查和解释结果，但必须标记判断依据与不确定性。必需条件失败不能被其他通过项抵消；未执行、缺证据、不适用不能混为一谈。

最终报告说明：客户要什么、检查了什么、依据在哪里、哪些目的满足、哪些失败、哪些尚不能证实、应修改或补充什么。结论只适用于本次材料和已经确认的范围，不作无限保证。

## 2026-09-22 v0.3 落地状态

主入口已改为通用材料审查。真实文字材料、目的及角色先形成字节快照；本机解析与计数后，SERV按客户目的审查内容、矛盾、遗漏和证据缺口；返回的目标片段、文件行号和原文被逐字核对，支持的工具再独立执行，最终合并成证据报告。原数据及网页检查入口保留，不是重做另一个项目。

静态公网演示的SERV回复是明确标注的真实录制，每次实际重算文件与引用。自有材料可经已登录模型网页转交，亦可由已授权AI助手完成。应用的助手任务队列/返回接口已有测试，但不能据此声称无人值守自动连接任意登录网站已验证。API与浏览器记录也不是同一种验证。

此版本实现的是通用审查工作流和可用的首批证据方法，不声称对任意交付类型、完整生产系统或所有事实都能自动验证到底。外部事实、执行与最终商业效果缺证据时明确未验证；不会因为“通用”二字生成没有依据的通过证明。
